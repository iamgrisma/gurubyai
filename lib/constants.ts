// lib/constants.ts

/**
 * Platform fee charged for each booking (in credits)
 */
export const PLATFORM_FEE = 10;

/**
 * Maximum distance (in km) recommended for offline services
 */
export const MAX_RECOMMENDED_DISTANCE = 10;

/**
 * Buffer time (in minutes) between bookings for travel/transition
 */
export const BOOKING_BUFFER_TIME = 30;

/**
 * Maximum number of saved locations per user
 */
export const MAX_SAVED_LOCATIONS = 5;
