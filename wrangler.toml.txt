name = "guruba-connect"
main = "worker.js"
compatibility_date = "2024-09-23"

# Cloudflare Workers Assets configuration
# This binds the ./dist folder (your build output) to the ASSETS binding in the worker
[assets]
directory = "./dist"
binding = "ASSETS"
